{
  "name": "oauth-handler",
  "version": "1.0.0",
  "description": "OAuth handler Lambda function for Experta social media connections",
  "main": "handler.js",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/client-secrets-manager": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "fast-check": "^3.12.0"
  }
}
