# ONZO - Frontend Environment Variables
# Environment: production

# Development Mode - MUST be false in production
# This ensures brand association checks are enforced
VITE_DEV_MODE=false

# Note: Other environment variables should be set via deployment configuration
# API Gateway, Cognito, and AWS Region should be configured per environment
